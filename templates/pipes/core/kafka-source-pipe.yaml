- route:
    from:
      uri: "kamelet:kafka-source"
      parameters:
        bootstrapServers: "The Bootstrap Servers"
        topic: "The Topic Names"
        saslAuthType: "NONE"
      steps:
        - to:
            uri: "log:info"
