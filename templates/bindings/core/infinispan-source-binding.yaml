- route:
    from:
      uri: "kamelet:infinispan-source"
      parameters:
        cacheName: "The Cache Name"
        hosts: "The Hosts"
        password: "The Password"
        username: "The Username"
    steps:
      - to:
          uri: "log:info"
    