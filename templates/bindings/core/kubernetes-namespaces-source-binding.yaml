- route:
    from:
      uri: "kamelet:kubernetes-namespaces-source"
      parameters:
        masterUrl: "The Kubernetes Master URL"
        token: "The Oauth Token"
      steps:
        - to:
            uri: "log:info"
