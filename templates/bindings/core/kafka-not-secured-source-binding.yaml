- route:
    from:
      uri: "kamelet:kafka-not-secured-source"
      parameters:
        bootstrapServers: "The Bootstrap Servers"
        topic: "The Topic Names"
      steps:
        - to:
            uri: "log:info"
