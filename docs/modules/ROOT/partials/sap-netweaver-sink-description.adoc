== SAP NetWeaver Sink Kamelet Description

=== The Command

The `command` property is the OData command to run against the gateway, and it becomes the
request path.

The component reads the command from the `CamelNetWeaverCommand` header, which this
Kamelet sets from the property. Setting it also overwrites any inbound value, so a caller
cannot choose the request path.
